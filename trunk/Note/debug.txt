1. dynamic_cast运行时出错













1.
在实用显示的强制类型
dynamic_cast
的时候，在VC6.0下需要设置，才可以使用。
工程（projece)->设置（setting)
在里面知道C/C++那个选项。
然后在下面分类中选
C++语言

然后在
勾中下面一个选项
叫允许时间类型信息（RTTI）
然后确定就可以使用了。
否则的话，是不可以使用的，运行会出错。